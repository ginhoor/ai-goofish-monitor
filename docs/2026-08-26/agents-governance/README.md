# AGENTS 分支生命周期治理

## 结果

- 为默认分支 `master` 增加前置、统一、强制的分支生命周期合同。
- 安全功能分支必须完成默认分支合并与 push、已合并远端/本地分支删除、任务 worktree 移除和 ref 验证后，才能声明完成。
- 仅 commit 或 push 明确属于未完成，除非用户限定交付边界。
- 项目原有部署、测试和安全细节继续保留。
- 不使用用户级 `~/.codex/AGENTS.md` 共用规则层；本仓库自行持有适用规则。
- 根 AGENTS.md 从 3085 字节调整为 5103 字节，保持在 24 KiB 内。

## 验证

- `git diff --cached --check`
- 文件前部存在统一强制条款
- 可选自动合并表述已移除
- 根 AGENTS.md 小于 24 KiB

## 更新日志

- 2026-08-26：统一强制合并、分支删除和 worktree 清理语义。
